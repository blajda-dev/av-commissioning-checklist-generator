<Window x:Class="CommissioningChecklistGenerator.AddEditTask"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:extension="clr-namespace:CommissioningChecklistGenerator.Extensions"
        xmlns:av="clr-namespace:CommissioningChecklistGenerator.AVSystem"
        xmlns:task="clr-namespace:CommissioningChecklistGenerator.Tasks"
        mc:Ignorable="d"
        Title="AddEditTask" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height=".75*"/>
            <RowDefinition Height="4*"/>
            <RowDefinition Height=".5*" MaxHeight="45"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <GroupBox x:Name="TaskType" Header="Task Type" Grid.ColumnSpan="2">
            <ListBox x:Name="TaskTypes" ItemsSource="{Binding Source={extension:EnumerationExtension {x:Type task:TaskType}}}" SelectedItem="{Binding TaskType, Mode=TwoWay}" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel IsItemsHost="True" Orientation="Horizontal" HorizontalAlignment="Stretch"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <RadioButton GroupName="ControlType" Content="{Binding}" HorizontalContentAlignment="Stretch" Click="OnTaskTypeChecked"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>
        <GroupBox x:Name="TaskMediaType" Grid.Row="1" Header="Task Media Type" Grid.ColumnSpan="2">
            <ListBox x:Name="MediaTypes"  ItemsSource="{Binding Source={extension:EnumerationExtension {x:Type av:MediaType}}}" SelectedItem="{Binding MediaType, Mode=TwoWay}">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel IsItemsHost="True" Orientation="Horizontal" HorizontalAlignment="Stretch"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <RadioButton GroupName="MediaType" Content="{Binding}" HorizontalContentAlignment="Stretch" Click="OnTaskMediaTypeChecked"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>
        <GroupBox x:Name="TaskTemplateTextGroupbox" Grid.Row="2" Header="Task Template Text" Grid.ColumnSpan="2">
            <TextBox x:Name="TaskTemplateText" Margin="5"/>
        </GroupBox>
        <Button x:Name="CompleteButton" Grid.Row="3" Grid.Column="1" Margin="5" Content="Add Task" Click="OnCompleteButtonClicked"/>
    </Grid>
</Window>
